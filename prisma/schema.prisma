// Church Management System — Prisma schema
// Models are added in Phase 1 (Database & Auth Foundation).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
